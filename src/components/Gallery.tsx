"use client";
import { useRef, useState } from "react";
import ShopImage from "./ShopImage";
const photos = [{ name: "front", caption: "Your local on High Road" }, { name: "interior", caption: "Take a look around" }, { name: "grocery", caption: "Cupboard staples & world foods" }, { name: "chilled", caption: "Chilled essentials" }, { name: "pets", caption: "Something for the pets" }, { name: "slush", caption: "A little chill break" }];
export default function Gallery() { const dialog = useRef<HTMLDialogElement>(null); const [selected, setSelected] = useState(photos[0]); return <><div className="gallery-grid">{photos.map(p => <button className="gallery-tile" key={p.name} onClick={() => { setSelected(p); dialog.current?.showModal(); }} aria-label={`Enlarge photo: ${p.caption}`}><ShopImage name={p.name} alt={p.caption} sizes="(max-width: 700px) 45vw, 33vw"/><span>{p.caption}<span aria-hidden="true">↗</span></span></button>)}</div><dialog ref={dialog} className="photo-dialog" aria-label={selected.caption} onClick={e => { if (e.target === e.currentTarget)
    dialog.current?.close(); }}><button className="dialog-close" onClick={() => dialog.current?.close()} autoFocus>Close ×</button><ShopImage name={selected.name} alt={selected.caption} sizes="(max-width: 960px) 90vw, 860px"/><p>{selected.caption}</p></dialog></>; }
