import { categories, shop } from "@/lib/shop";
import ShopImage from "@/components/ShopImage";
export default function Products() { return <div className="wrap"><section className="page-intro"><p className="eyebrow">WHAT’S IN STORE</p><h1>Your everyday,<br /><span>all in one local.</span></h1><p className="intro">From a fridge top-up to something for the pets. Explore our range, then pop in and see what’s on the shelves.</p><p className="muted">Our range varies. Call us to check a particular item before making a special trip.</p></section><nav aria-label="Jump to a range category" className="category-nav">{categories.map(c => <a href={`#${c.id}`} key={c.id}>{c.title}</a>)}</nav><div className="category-sections">{categories.map((c, i) => <section id={c.id} key={c.id} className="category-section"><ShopImage name={c.image} alt={c.alt}/><div><p className="eyebrow">IN STORE / {String(i + 1).padStart(2, "0")}</p><h2>{c.title}</h2><p>{c.description}</p>{c.id === "slushies" && <p className="price-note">Coco’s slushies · £1.20</p>}<a className="text-link" href={shop.telephone}>Ask us about availability →</a></div></section>)}</div><section className="callout"><h2>Looking for something specific?</h2><p>Give the shop a call and we’ll check what’s available before you make a trip.</p><a className="button" href={shop.telephone}>Call {shop.phone}</a></section></div>; }
